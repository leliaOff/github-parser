import { Observable } from 'rxjs';

export class Page {
    
    //searchInput(): Observable<any> {
        //return Observable.fromEvent(document.querySelector('input[name=search]'), 'input');
    //}

}